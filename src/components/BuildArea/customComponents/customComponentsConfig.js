// Add type of custom component here
// Its for mark delete, to recognize that custom component cannot be returned to default list (UIBlock or Title)
const customComponentsTypes = ["line"];

export default customComponentsTypes;
